# Upstream resources

Future external resources must be declared with immutable version or revision pins. Normal setup
must never follow upstream `main` or `HEAD` blindly. Before vendoring, review the source license,
redistribution and modification rights; keep provenance and attribution with each controlled
adaptation. Updates require review.

Phase 1 fetches, installs, and vendors no upstream resources.
