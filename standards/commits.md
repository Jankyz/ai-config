# Commit standard

Creating a Git commit requires explicit owner authorization after implementation and verification are complete.

Use one coherent Lore commit that explains intent and preserves meaningful constraints, rejected approaches, confidence, scope risk, directives, verification, and known gaps.

Stage only the intended scope. Inspect status and diff before committing, validate the commit message, and never push, tag, publish, or release unless separately authorized.
