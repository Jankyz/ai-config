interface:
  display_name: "AI Config Review"
  short_description: "Read-only review of an implementation or diff."
  default_prompt: "Use $aic-review to review this implementation against its requirements."
policy:
  allow_implicit_invocation: false
